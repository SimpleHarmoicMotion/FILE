using System;

namespace Mysys
{
    public class Cls1
    {
        public int AddNum(int a, int b)
        {
            int Sum = Math.Abs(a) + Math.Abs(b);
            return Sum;
        }
    }
}