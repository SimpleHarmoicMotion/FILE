int a = (x + y - z) * (b / c) % d;

int b = 100;
a += a;

int i = 1;
i++;

if((a > 1 && b < 0) || c == 1 || !d)
if(a <= b)

byte e = 7;
byte f = (e & 5);

int g = 2;
g = g << 5;

int val = (a > b) ? a : b;
string s = str ?? "(NULL)";

int? h = null;
h = h ?? 0;

string s = null;
s = s ?? string.Empty;